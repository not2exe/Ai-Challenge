Boot iPhone 15 simulator,
build app from PROJECT_PATH (scheme: SCHEME_NAME),
install and launch it,
take screenshot,
find and tap element with label containing "challenge",
wait 2 seconds,
take another screenshot,
then send both screenshots to Telegram with captions "📱 Initial App State" and "🎯 Challenge Detail View"
plus a summary message showing build status and bundle ID.


Remember to replace PROJECT_PATH and SCHEME_NAME with your actual values.
Then paste this into chat and press Enter twice to submit.